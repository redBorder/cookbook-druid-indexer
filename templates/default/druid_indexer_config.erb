<%# Generated by chef-client (rb-druid-indexer) %>
zookeeper_servers:
<% @zookeeper_servers.each do |server| %>
  - "<%= server %>"
<% end %>

<%# Define tasks %>
tasks:
<% @tasks.each do |task| %>
  - task_name: "<%= task[:task_name] %>"
    namespace: "<%= task[:namespace] %>"
    feed: "<%= task[:feed] %>"
    kafka_host: "<%= task[:kafka_host] %>"
<% end %>